package dsa.week6;

public class Selection_Sort {
	
	public int[] sorter(int[] input)
	{
		for (int i = 0; i < input.length-1; i++) {
			for (int j = i+1; j < input.length; j++) {
				if(input[j]>input[i]) {
					
				}
					
			}
		}
		return input;
	}
	

}

/*
 Scan/Iterate through the array 
 swap the current index with next smallest num in the iteration 
 */
